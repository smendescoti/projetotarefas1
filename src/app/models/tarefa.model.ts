//modelo de dados
export interface Tarefa {
    //atributos de tarefa (campos)
    idTarefa: number,
    nome: string,
    data: string,
    hora: string,
    descricao: string,
    prioridade: string
}
